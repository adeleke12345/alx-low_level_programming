#include <stdio.h>

/**
 *
 *This is the main - Entry point
 *It always return: 0 for (Successful) execution
 *
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
